# In development this can be left empty — Vite proxy handles /api → http://localhost:5000
# In production on Render, render.yaml populates this automatically with the backend Web Service URL
VITE_API_URL=https://pharmpos-api.onrender.com
