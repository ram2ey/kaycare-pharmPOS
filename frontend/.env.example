# In development this is empty — Vite proxy handles /api → localhost:5000
# In production set this to the Azure App Service URL (no trailing slash)
VITE_API_URL=https://kaycare-prod-api.azurewebsites.net
